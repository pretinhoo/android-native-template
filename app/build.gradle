plugins {
    id 'com.android.application'
}

android {
    namespace "com.appsforsusers.hello"
    compileSdk 34

    defaultConfig {
        applicationId "com.appsforsusers.hello"
        minSdk 21
        targetSdk 34

        ndk {
            abiFilters "arm64-v8a", "armeabi-v7a"
        }
    }

    // 🔴 AQUÍ SÍ VA EL path (y SOLO aquí)
    externalNativeBuild {
        cmake {
            path "src/main/cpp/CMakeLists.txt"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
}
